# REVIEW-006: REVIEW-005 fix-loop re-review

**Date:** 2026-08-18
**Reviewer:** Codex Sol (reviewer of record, ultra effort, fresh session)
**Target:** `feat/app-skeleton` at
`c59b932d1e9f387375aa4cbb72fd402418de9e53`, delta from
`a5258d77ac963a769707c34e093107c9c4b37178`
**Verdict:** FAIL

> Immutable. Once committed, do not edit. A later re-review is a new record.

## Scope

Reviewed only the supplied fix commit
`a5258d77ac963a769707c34e093107c9c4b37178...c59b932d1e9f387375aa4cbb72fd402418de9e53`.
Both full SHAs resolve, the supplied base is the target's sole parent and
merge-base, the range contains exactly one commit, and the checked-out branch,
`origin/feat/app-skeleton`, and the fetched remote head all resolved to the
exact target before review. Matters settled in REVIEW-005 and earlier were not
reopened.

On the clean checked-out branch at the target, I ran the committed `npm-ci.sh`,
the stability gate once by itself, and the committed negative control. The
install completed 1,085 packages and encoded npm exit 0. The standalone gate
reported eleven gated artifacts, zero differences, and process exit 0. The
negative control then reported exactly one `DIFFERS` with process exit 1,
restored the controlled bytes, and reported all eleven identical with process
exit 0. Before/after fingerprints independently confirmed exact restoration of
all tracked working-copy bytes and index entries; both cached and unstaged
diffs were empty afterward.

This was a repository-only review. I did not query Supabase or any deployment,
fix reviewed work, merge, or open a PR. The review changes only this immutable
record and the required HANDOFF append.

Controller confirmations applied without findings: the authorized
PROJECT-STATE shape change; `npm-ci.sh` being a run-varying capture whose own
process status is not its contract; the documented abnormal-exit restore limit
of the negative control; the accepted six-path `git-ls-files.txt` delta; and all
prior advisories, dependency, Node-skew, CI, device-rendering, and `index`
chrome deviations.

## Findings

| # | Severity | Classification | File:line at `c59b932` | Finding | Status |
|---|---|---|---|---|---|
| 1 | low | FAIL introduced by this work | `docs/05-quality/evidence/002d-fix-loop-3/npm-ci.sh:9-20,28-33`; `docs/05-quality/evidence/002d-fix-loop-3/README.md:66-74` | **The install evidence producer does not reliably apply its stated duration-only normalization.** Its comments and README say the wall-clock suffix on npm's `added ...` summary is masked to `<duration>`, but the regex only matches the form containing `, and audited ... packages`. The fresh run at this exact head produced the valid summary `added 1085 packages in 2m`; the script left `2m` unmasked. A direct two-form probe confirmed that the audited form is masked while this shorter form passes unchanged. The install itself remains valid and encoded exit 0; this finding is about the new producer's normalization contract, not install success or byte stability of an artifact explicitly classified run-varying. | open; verdict-driving |

## REVIEW-005 finding status

| Prior finding | Classification | Reviewer evidence | Status |
|---|---|---|---|
| 1 — false-green stability process | PASS | The standalone committed gate returned 0 with 11/11 identical. The committed negative control reproduced one `DIFFERS`, encoded and process exit 1, then 11/11 identical, encoded and process exit 0. | fixed |
| 2 — unsupported install PASS | PASS with new finding 1 | The committed script invokes real `npm ci`; [npm-ci.txt](../05-quality/evidence/002d-fix-loop-3/npm-ci.txt) records 1,085 packages and encoded exit 0, and OPERATIONS cites it directly. The fresh script run repeated 1,085 packages and encoded exit 0. Finding 1 is the producer's uncovered duration form, not the former unsupported install claim. | fixed; new finding 1 open |
| 3 — stale authoritative state | PASS | PROJECT-STATE contains no raw gated/run-varying totals, points to [the owning evidence section](../05-quality/evidence/002b-fix-loop/README.md#gated-versus-run-varying), and its exact delta is one deletion plus one insertion on only the Unit A Active-work row. The controller confirms this shape was authorized. | fixed |
| 4 — three false/stale prose statements | PASS | The module-count statement, four-artifact count, and dev-server provenance are corrected at their sources. The gate regenerated the affected transcripts byte-identically from the committed scripts. | fixed |
| 5 — generated trailing whitespace | PASS | The producer uses `paste` without a trailing delimiter, [export-summary.txt](../05-quality/evidence/002b-fix-loop/export-summary.txt) regenerated byte-identically, and both two-dot and three-dot `git diff --check` returned 0. | fixed |

## Evidence and classifications

| Check | Classification | Reviewer evidence |
|---|---|---|
| Exact target, remote and range | PASS | Local HEAD, `origin/feat/app-skeleton`, and fetched remote were `c59b932`; merge-base and sole parent were `a5258d7`; the range is one non-empty commit. |
| Fresh lockfile install | PASS | Running the committed [npm-ci.sh](../05-quality/evidence/002d-fix-loop-3/npm-ci.sh) completed 1,085 packages and wrote encoded npm exit 0. The tracked run-varying transcript was restored afterward. |
| Install normalization | FAIL introduced by this work | Fresh summary `added 1085 packages in 2m` retained its raw duration; the source regex masks only the audited-summary form. Finding 1. |
| Standalone stability gate | PASS | Unchanged [stability.sh](../05-quality/evidence/002c-fix-loop-2/stability.sh) reported 11 gated, 0 differing, and returned process exit 0; its output matched [stability.txt](../05-quality/evidence/002c-fix-loop-2/stability.txt). |
| Stability negative control | PASS | Unchanged [negative-control.sh](../05-quality/evidence/002d-fix-loop-3/negative-control.sh) reproduced the committed [negative-control.txt](../05-quality/evidence/002d-fix-loop-3/negative-control.txt) byte-for-byte: exactly one `DIFFERS`, process 1, then all identical, process 0. |
| Negative-control restoration | PASS | Index-entry fingerprint before/after: `afd2bd05d7f37ac370560dcd6dd2c48a76b5e98596f12b07efa61f836b320b45`. Tracked-worktree fingerprint before/after: `16ad26257bbf5cfadd11126e1ca9c6b35f1a01850dc82374278c93b14a2eb798`. The control blob stayed `d784e7ac49d735097f65cc4e9baf542efe43961c`; cached and unstaged diffs both returned clean. |
| Local CI transcripts inside gate | PASS | Typecheck, lint, test and format-check artifacts all regenerated byte-identically and retain encoded exit 0. |
| OPERATIONS install row | PASS | It cites `002d-fix-loop-3/npm-ci.txt` directly and accurately limits the claim to lockfile installation. |
| PROJECT-STATE content and boundary | PASS | No raw gate totals remain; the pointer resolves to the 11-gated/4-run-varying section. Exact numstat is `1 1`, and a zero-context diff shows only the Unit A row. |
| Three prose corrections and generated outputs | PASS | Exact source corrections are in `capture.sh`, the 002c README, and `dev-server.sh`; the stability rerun reproduced `export-summary.txt` and `dev-server.txt` byte-for-byte. |
| Accepted tracked-file delta | PASS | The six additions are exactly REVIEW-005 plus the five 002d paths; count 79 becomes 85. |
| Delta whitespace | PASS | `git diff --check` in both exact two-dot and merge-base three-dot forms, plus `git show --check c59b932`, returned 0. The trailing-space producer and output are both corrected. |
| Governance preservation | PASS | HANDOFF is 74 insertions/0 deletions; BRANCH-NOTES is 59 insertions/0 deletions; no `docs/03-decisions/` or `docs/04-reviews/` path changed in the builder delta. The LOCK remains `REVIEW`. |
| CI execution | NOT RUN | No PR was opened; controller-accepted and unchanged. |
| Simulator/emulator/device rendering | NOT RUN | Explicitly accepted for Unit A; the owner web PASS stands. |

## Standards

No standards finding in the reviewed fix delta. The state and handoff changes
respect their authorized boundaries, immutable paths are untouched, and the
five remedies stay within the dispatched scope. No baseline code smell is
reported; repeated fix-loop prose is required by immutable governance history.

## Spec

One low specification/evidence-source finding: `npm-ci.sh` does not mask the
duration in a valid summary form observed during the required current-head
install, despite the source and README saying that duration is the single
normalized field. The former unsupported-install finding is otherwise fixed,
and the other four REVIEW-005 remedies are complete. No scope creep was found.

Standards: 0 findings. Spec: 1 low finding; worst is the uncovered npm summary
duration form.

## Carried items

The 22 accepted audit advisories, retained navigation dependencies, Node 26 / CI
Node 24 skew, CI NOT RUN, simulator/emulator/device rendering NOT RUN,
run-varying `npm-ci.sh` process-status deviation, negative-control abnormal-exit
restore limitation, six-path tracked-list delta, and `index` chrome backlog item
remain accepted or deferred, not findings.
