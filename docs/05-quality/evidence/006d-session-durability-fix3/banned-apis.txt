# Banned authentication surfaces must not appear in application source.
# Scope: src/ (tracked application code), THESE LITERAL PATTERNS through
# the strip_comments pipeline — nothing broader is claimed. Each pattern
# is checked against src/, then against a synthetic control file that
# DOES contain it — a pattern that fails its control fails the run.

source files scanned: 14

signInWithPassword       src/ code hits: 0    (control MATCHED)
signUp                   src/ code hits: 0    (control MATCHED)
resetPasswordForEmail    src/ code hits: 0    (control MATCHED)
signInWithOAuth          src/ code hits: 0    (control MATCHED)
signInWithIdToken        src/ code hits: 0    (control MATCHED)
signInWithSSO            src/ code hits: 0    (control MATCHED)
emailRedirectTo          src/ code hits: 0    (control MATCHED)
magiclink                src/ code hits: 0    (control MATCHED)
secureTextEntry          src/ code hits: 0    (control MATCHED)
linkIdentity             src/ code hits: 0    (control MATCHED)

## The OTP surface that IS used, for contrast
src/app/(app)/index.tsx:9:  const { state, signOut } = useAuth();
src/app/(app)/index.tsx:18:    const { error: signOutError } = await signOut();
src/app/(app)/index.tsx:25:    if (signOutError) {
src/app/(app)/index.tsx:26:      setError(signOutError.message);
src/app/(auth)/sign-in.tsx:16:  const { sendOtp, verifyOtp } = useAuth();
src/app/(auth)/sign-in.tsx:40:    const { error: verifyError } = await verifyOtp(email.trim(), code.trim());
src/lib/auth/secure-store-adapter.ts:661:    // does not emit SIGNED_OUT, `signOut()` rejects, `auth-provider.tsx`
src/lib/auth/foreground-refresh.ts:19: * refreshes from construction, from session loading, and from `signOut()`,
src/lib/auth/auth-provider.tsx:37: * instance — straight out of `verifyOtp`/`signOut`. A caller that only expected
src/lib/auth/auth-provider.tsx:53:  readonly verifyOtp: AuthAction<[email: string, token: string]>;
src/lib/auth/auth-provider.tsx:54:  readonly signOut: AuthAction<[]>;
src/lib/auth/auth-provider.tsx:103:  // the `verifyOtp` action (which is where a fresh sign-in is known to have
src/lib/auth/auth-provider.tsx:202:        // A dropped sign-in is not lost: the app's own `verifyOtp` resolves
src/lib/auth/auth-provider.tsx:233:     * `signOut({ scope: 'local' })` asks; the read-back answers. The return
src/lib/auth/auth-provider.tsx:237:     * finding 3 showed what any weaker rule invites: pinned `signOut()` can
src/lib/auth/auth-provider.tsx:251:        await supabase.auth.signOut({ scope: 'local' });
src/lib/auth/auth-provider.tsx:255:        // the verdict. (The user-facing `signOut` action is a different path
src/lib/auth/auth-provider.tsx:325:     * `verifyOtp` success — the one entrance that mints a new session; no
src/lib/auth/auth-provider.tsx:482:      const { error } = await supabase.auth.signInWithOtp({
src/lib/auth/auth-provider.tsx:490:  const verifyOtp = useCallback(async (email: string, token: string) => {
src/lib/auth/auth-provider.tsx:492:      const { error } = await supabase.auth.verifyOtp({ email, token, type: 'email' });
src/lib/auth/auth-provider.tsx:502:  const signOut = useCallback(async () => {
src/lib/auth/auth-provider.tsx:513:      const { error } = await supabase.auth.signOut({ scope: 'local' });
src/lib/auth/auth-provider.tsx:519:    () => ({ state, sendOtp, verifyOtp, signOut }),
src/lib/auth/auth-provider.tsx:520:    [state, sendOtp, verifyOtp, signOut],
src/lib/auth/session-storage.ts:152: * that invites: `signOut()` refreshes the residual on its way out
src/lib/auth/session-storage.ts:263: * returns true only when every read succeeded and found nothing. A `signOut()`
src/lib/supabase.ts:54:    // that `signOut()` loads, and can refresh, the stored session before
src/lib/supabase.ts:59:    // from `signOut()`, and from paths not yet identified — are recorded,
