# Evidence — 002c REVIEW-004 fix loop (Unit A, CTRL-002)

Branch `feat/app-skeleton`, same builder, same LOCK, `Status: REVIEW`
throughout. Base for this loop is the REVIEW-004 record commit
`52061c95b660b5efc39d558da04563da9a6e0aaf`.

This directory covers **only** the two REVIEW-004 findings. Nothing settled in
REVIEW-003 or earlier was reopened, and no controller ruling was relitigated.

## Where this loop's evidence lives, and why

The dispatch left the layout to the builder. The call:

| Kind of change | Where it went | Why |
|---|---|---|
| The generating scripts that produce the 002b transcripts, and the transcripts themselves | **amended in `../002b-fix-loop/`** | Finding 1 is that those exact artifacts do not reproduce. A fix that leaves the broken copies in place and puts corrected ones next door would leave the failing files still committed, and a reader following `REVIEW-004`'s file citations would land on them. The scripts were changed; every output was regenerated by running them; nothing was hand-edited. The `## Byte stability` section of that directory's README records what moved and what was done about it. |
| The proof that they now reproduce | **here** — `stability.sh`, `stability.txt` | It is this loop's result, not the previous loop's. |
| The one runtime fact `OPERATIONS.md` newly claims | **here** — `dev-server.sh`, `dev-server.txt` | New evidence, produced by this loop. |
| The empty slot for the owner's rendering attestation | `../002c-owner-smoke/` | Named by the dispatch. Nothing of this loop's belongs in it — it stays empty until a human fills it. |

Nothing under `../002a-app-skeleton/` changed in this loop.
`../002a-app-skeleton/git-ls-files.txt` is regenerated by the same committed
script as before and is included in the gate below, but its content at this head
is what it always was: a listing of the tracked tree.

## Finding 1 — byte-stable regeneration

REVIEW-004 found that four of the twelve 002b transcripts changed when the
committed scripts were rerun at the committed head, without any network being
involved. Each carried a field that moves on its own.

| Artifact | What moved | Fix, in the script |
|---|---|---|
| `test.txt` | Jest's per-test duration, its `Time:` total, and the slow-suite duration Jest appends to the `PASS` line only when a suite takes over 5 s | `capture.sh` replaces the first two with `<duration>` and drops the third |
| `expo-export.txt` | Metro's four `Bundled <n>ms` durations, and a cold-cache warning that appears only on a machine with no Metro cache | `capture.sh` replaces the durations and drops the warning |
| `name-scan.txt` | the section-4 count of governance files naming the project — it read the working tree mid-session, so it recorded 14 for a commit holding 21 | `name-scan.sh` section 4 reads the index (`git grep --cached`), which is what is about to be committed and equals HEAD at any committed head |
| `push-state.txt` | `git rev-parse origin/feat/app-skeleton`, and the ahead/behind count against HEAD | `fix-state.sh` prints no moving SHA at all; it asks whether the remote branch *contains* each already-reviewed commit, which is permanent once true |

Proving the gate turned up a fifth artifact with the same defect, which
REVIEW-004 did not flag because its sandbox never generated the file that
causes it:

| Artifact | What moved | Fix, in the script |
|---|---|---|
| `lint-file-list.txt` | whether the generated, gitignored `expo-env.d.ts` was on disk. ESLint inspects it when it is there, so the listing reads 5 files in a fresh clone and 6 once any `expo` command has run — including this directory's own export step | `capture.sh` lists only tracked files, and reports problems found in untracked ones as a separate count (`0`). Nothing is hidden: `lint.txt` is the gate and still covers everything ESLint sees. |

Two of the three run-varying artifacts proved the classification in practice
rather than in theory, during this loop's reruns.

`expo-doctor.txt` returned three different transcripts across the eight runs of
the same command this loop made against the same tree: **21/21** five times,
**20/21** twice with
*"Validate packages against React Native Directory — Directory check failed with
unexpected server response"*, and once a different failure entirely,
*"Check Expo config schema … TypeError: fetch failed … getaddrinfo ENOTFOUND
exp.host"*. Both failures are the reachability of a service Expo queries, and
neither says anything about this repository. The committed transcript is a
21/21 run, which is the project's actual compatibility status; a reviewer who
gets 20/21 with either message above has hit the same upstream flake, not a
regression.

`npm-audit.txt` reordered part of its dependency tree between runs and changed
which breaking upgrade it suggests (`expo@53.0.27` one run, `react-native@0.72.17`
the next), while reporting the same **22 advisories (7 moderate, 15 high)**.

Neither file would have survived a byte gate, and neither should have been made
to.

Nothing that carries signal was normalised away: exit codes, pass/fail counts,
module counts, bundle sizes, bundle content hashes, route lists, and the
three-depth name-scan result all pass through untouched. What was removed is
wall-clock time and one cache-state warning.

**Three artifacts cannot be made byte-stable and are classified run-varying**
rather than forced. Each is named with the exact fields that vary, in
`../002b-fix-loop/README.md`: `environment.txt` (the `node`, `npm` and `os`
lines — machine-local by design), `expo-doctor.txt` (the tool build resolved
from `@latest`, and its check count) and `npm-audit.txt` (advisory count and
severities, which come from the upstream database). The byte-stability claim is
scoped to the remaining ten, and `stability.txt` is that claim re-proven at this
head.

**One limit is stated rather than engineered around.** `push-state.txt` cannot
attest that its own commit is pushed — no artifact inside a commit can name that
commit's hash. It attests every commit up to and including the REVIEW-004
record, which is the whole of what REVIEW-003 finding 3 concerned. The push of
the commit carrying it is verified by the reviewer fetching.

## Finding 2 — OPERATIONS.md runtime claims

The file said a fresh clone "runs", that a local Expo dev-server environment
"exists", and that the app skeleton "runs", while the same file classified
rendering as NOT RUN. Those are now three separate statements, each with its own
class and artifact, in a table under **What is actually proven, and what is
not**:

| Statement | Class |
|---|---|
| `npm ci` installs the committed lockfile | PASS |
| `expo export --platform all` produces iOS, Android and web bundles | PASS |
| The dev server starts and answers HTTP 200 on `/`, with the placeholder screen's own strings in the markup it serves | PASS — new in this loop, `dev-server.txt` |
| The app renders on a browser, simulator, or device | **NOT RUN** |

The environments table's local row no longer says the skeleton "runs"; it says
what it does — installs, bundles, serves `/` — and that it is not yet rendered
on any target. `npm run ios` and `npm run android` are now marked as never
having been executed.

`dev-server.txt` is the new artifact and it is careful about its own limits: the
markup it captures is produced by Expo Router's static rendering inside Node.
No browser laid out a page and no device mounted a view. It also records that
`ZC App (dev)` does **not** appear in what the web target serves — the skeleton
leaves the document title empty — which is why the smoke procedure sends anyone
who wants to see the name to the Expo Go target.

A short **Owner smoke test** section was added to `OPERATIONS.md` with the
`npm ci` / `npm run web` procedure and the expected result, and
`../002c-owner-smoke/` was created as the slot its result lands in.

## Claims

| # | Claim | Class | Artifact |
|---|---|---|---|
| 1 | The ten gated 002b/002a artifacts regenerate byte-for-byte from their committed scripts at this head | PASS | `stability.txt` |
| 2 | The three artifacts that cannot be byte-stable are classified run-varying, each naming the fields that vary | PASS | `stability.txt`, and the table in `../002b-fix-loop/README.md` |
| 3 | No evidence output was edited by hand — every artifact came from running its committed script | PASS | the diff: the four scripts changed, and the transcripts changed only in the ways those scripts produce |
| 4 | The dev server starts and serves the root route with HTTP 200 | PASS | `dev-server.txt` |
| 5 | The markup it serves contains the placeholder screen's own strings | PASS | `dev-server.txt` section 2 |
| 6 | `OPERATIONS.md` makes no runtime claim beyond claims 4 and 5 and the export | PASS | the `OPERATIONS.md` diff |
| 7 | The owner smoke procedure and its evidence slot exist | PASS | `OPERATIONS.md` **Owner smoke test**, `../002c-owner-smoke/README.md` |
| 8 | Typecheck, lint, test and format:check still exit 0 | PASS | `../002b-fix-loop/typecheck.txt`, `lint.txt`, `test.txt`, `prettier-check.txt` |
| 9 | The app still bundles for iOS, Android and web | PASS | `../002b-fix-loop/expo-export.txt` |
| 10 | Dependencies still match what Expo SDK 57 expects | PASS | `../002b-fix-loop/expo-doctor.txt` (21/21) |
| 11 | `npm audit` is unchanged by this loop | FAIL pre-existing | `../002b-fix-loop/npm-audit.txt` |
| 12 | CI runs install → typecheck → lint → test → format:check on PR and push-to-main | NOT RUN | still no PR; this loop adds a commit, not a trigger |
| 13 | The gates fail on a real violation and return to green | NOT RUN | not re-run; no gate, config or script the 002a negative control exercises was touched |
| 14 | The app renders on a device, simulator, or browser | NOT RUN | `../002c-owner-smoke/` — the slot, deliberately empty |

## Re-running this

From the repository root, after `npm ci` and `git fetch origin`:

- `stability.sh` — the gate. Regenerates every artifact from its committed
  script, compares the gated set against the copies in the Git index, prints any
  difference, then restores the directory from the index so that checking the
  work never rewrites it. Takes several minutes; it runs a full three-platform
  export. It requires the artifacts to be staged, because it reads the index —
  at a committed head that is the same thing as HEAD.
- `dev-server.sh` — `dev-server.txt`. Starts the dev server on port 8081,
  probes `/`, and kills it. Stops rather than running if port 8081 is already
  held, so that the transcript cannot silently record a different port.
