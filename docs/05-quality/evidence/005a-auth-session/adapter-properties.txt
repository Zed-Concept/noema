# The storage layer's properties, one line per assertion: the chunked
# adapter itself, then the platform split that decides where it is used.
# Runs against an in-memory double: no device, no native module, no
# credentials. See README.md for what the double does and does not model.


> noema@0.1.0 test
> jest --ci --verbose --testPathPattern secure-store-adapter|session-storage-platform

PASS src/__tests__/session-storage-platform.test.ts
  session storage — platform split
    ✓ gives web no adapter, so supabase-js falls back to localStorage
    ✓ gives iOS the chunked SecureStore adapter
    ✓ gives Android the chunked SecureStore adapter

PASS src/__tests__/secure-store-adapter.test.ts
  chunked SecureStore adapter — round trip
    ✓ returns a payload past the chunk threshold byte-for-byte identical
    ✓ actually splits that payload across multiple chunk keys
    ✓ keeps every stored chunk within the byte budget
    ✓ round-trips multi-byte text without corrupting it
    ✓ never leaves a lone surrogate in a stored chunk
    ✓ round-trips an empty string and reports an absent key as null
  chunked SecureStore adapter — fails closed
    ✓ returns null, not a truncated prefix, when a middle chunk is gone
    ✓ returns null when the final chunk is gone
    ✓ returns null when the index is corrupt
    ✓ returns null for a plain value written by something other than this adapter
    ✓ returns null when the reassembled length disagrees with the index
    ✓ resolves null instead of rejecting when the backend throws
    ✓ writes the index last, so an interrupted write is never readable
    ✓ is unreadable if the write stopped before the index landed
  chunked SecureStore adapter — leaves nothing behind
    ✓ removes every chunk and the index, leaving no key at all
    ✓ leaves no key behind even when the index was corrupted first
    ✓ leaves no key behind when a chunk was deleted before sign-out
    ✓ orphans no chunk when a longer value is replaced by a shorter one
    ✓ touches no other key while removing its own
  chunked SecureStore adapter — key derivation
    ✓ derives chunk keys deterministically from the base key and generation
    ✓ derives keys SecureStore will accept
    ✓ cannot collide with the sibling keys auth-js derives
  chunked SecureStore adapter — a reader never sees a gap
    ✓ exposes the old value or the new one at every point of a replacement, never null
    ✓ leaves only one generation behind after a replacement
    ✓ alternates generations so a write never touches the one being read
  chunked SecureStore adapter — teardown cannot reject
    ✓ resolves removeItem instead of rejecting when the backend fails
    ✓ still writes a value when only the cleanup reads and deletes fail
    ✓ purges chunks left in either generation

Test Suites: 2 passed, 2 total
Tests:       31 passed, 31 total
Snapshots:   0 total
Time:        <duration>
Ran all test suites matching /secure-store-adapter|session-storage-platform/i.
exit: 0
