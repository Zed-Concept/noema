# REVIEW-024 findings 1+2 probe — one probe, two trees.
# See review024-probe.tsx for what is asserted and review024-probe.sh for how.
#
# candidate: 5f6d2e6ca873ff3b45d9d9a6e52d42bdebed30bd (pinned; the formal head REVIEW-024 measured; must be RED)
# head:      4742aef1f492efb5a363fb3c33788f9966f1f51b (the fix candidate at the time of this run; must be GREEN)
# NOTE the head SHA above is the commit the probe ran against. The commit
# that adds this transcript necessarily post-dates it — the same
# head-cannot-name-itself boundary as ci.txt (REVIEW-022 claim 48a ruling).

==========================================================================
## RUN 1 — reviewed candidate 5f6d2e6ca873ff3b45d9d9a6e52d42bdebed30bd (expected: RED)
==========================================================================
FAIL src/__tests__/review024-probe.test.tsx
  REVIEW-024 findings 1 and 2 — the probe (RED at reviewed candidate 5f6d2e6c, GREEN at the head)
    ✕ finding 1 — a present record whose read throws is OUTSTANDING while exists lies false: purge, never signedIn
    ✓ finding 1 control — the identical thrown read with exists TRUE is outstanding the same way
    ✓ absence control — no record, empty listing, exists false: no demand, the stored session bootstraps
    ✕ finding 2 — fresh-sign-in resolution with a refused follow-up refresh ends signedOut with the new demand durable
    ✕ finding 2 — the mid-process resolution re-read with a refused follow-up persist ends signedOut, nothing exposed

  ● REVIEW-024 findings 1 and 2 — the probe (RED at reviewed candidate 5f6d2e6c, GREEN at the head) › finding 1 — a present record whose read throws is OUTSTANDING while exists lies false: purge, never signedIn

    expect(received).toBe(expected) // Object.is equality

    Expected: "signedOut"
    Received: "signedIn"

      408 |     await p1.rtl.act(drain);
      409 |
    > 410 |     await p1.rtl.waitFor(() => expect(hook.result.current.state.status).toBe('signedOut'));
          |                  ^
      411 |     expect(history.some((state) => state.status === 'signedIn')).toBe(false);
      412 |     // Honoured all the way: the purge ran and the read-back proved the
      413 |     // residual gone.

      at Object.waitFor (src/__tests__/review024-probe.test.tsx:410:18)
      at asyncGeneratorStep (<node_modules>/@babel/runtime/helpers/asyncToGenerator.js:3:17)
      at _next (<node_modules>/@babel/runtime/helpers/asyncToGenerator.js:17:9)

  ● REVIEW-024 findings 1 and 2 — the probe (RED at reviewed candidate 5f6d2e6c, GREEN at the head) › finding 2 — fresh-sign-in resolution with a refused follow-up refresh ends signedOut with the new demand durable

    expect(received).toBe(expected) // Object.is equality

    Expected: "signedOut"
    Received: "signedIn"

      522 |     // session the bootstrap promise carried — and the NEW demand is
      523 |     // durable. At the reviewed candidate this read signedIn here.
    > 524 |     await p1.rtl.waitFor(() => expect(hook.result.current.state.status).toBe('signedOut'));
          |                  ^
      525 |     expect(historyExposed(history, 'probe-access-rot')).toBe(false);
      526 |     expect(mockDemandStore.files.size).toBe(1);
      527 |

      at Object.waitFor (src/__tests__/review024-probe.test.tsx:524:18)
      at asyncGeneratorStep (<node_modules>/@babel/runtime/helpers/asyncToGenerator.js:3:17)
      at _next (<node_modules>/@babel/runtime/helpers/asyncToGenerator.js:17:9)

  ● REVIEW-024 findings 1 and 2 — the probe (RED at reviewed candidate 5f6d2e6c, GREEN at the head) › finding 2 — the mid-process resolution re-read with a refused follow-up persist ends signedOut, nothing exposed

    expect(received).toBe(expected) // Object.is equality

    Expected: "signedOut"
    Received: "signedIn"

      580 |     // publication was refused. No render after the resolution began ever
      581 |     // held a session. At the reviewed candidate this ended signedIn.
    > 582 |     await p1.rtl.waitFor(() => expect(hook.result.current.state.status).toBe('signedOut'));
          |                  ^
      583 |     expect(
      584 |       history.slice(beforeResolution).some((state) => state.status === 'signedIn'),
      585 |     ).toBe(false);

      at Object.waitFor (src/__tests__/review024-probe.test.tsx:582:18)
      at asyncGeneratorStep (<node_modules>/@babel/runtime/helpers/asyncToGenerator.js:3:17)
      at _next (<node_modules>/@babel/runtime/helpers/asyncToGenerator.js:17:9)

Test Suites: 1 failed, 1 total
Tests:       3 failed, 2 passed, 5 total
Snapshots:   0 total
Time:        <duration>
Ran all test suites matching /review024-probe/i.

exit: 1

==========================================================================
## RUN 2 — head 4742aef1f492efb5a363fb3c33788f9966f1f51b (expected: GREEN)
==========================================================================
PASS src/__tests__/review024-probe.test.tsx
  REVIEW-024 findings 1 and 2 — the probe (RED at reviewed candidate 5f6d2e6c, GREEN at the head)
    ✓ finding 1 — a present record whose read throws is OUTSTANDING while exists lies false: purge, never signedIn
    ✓ finding 1 control — the identical thrown read with exists TRUE is outstanding the same way
    ✓ absence control — no record, empty listing, exists false: no demand, the stored session bootstraps
    ✓ finding 2 — fresh-sign-in resolution with a refused follow-up refresh ends signedOut with the new demand durable
    ✓ finding 2 — the mid-process resolution re-read with a refused follow-up persist ends signedOut, nothing exposed

Test Suites: 1 passed, 1 total
Tests:       5 passed, 5 total
Snapshots:   0 total
Time:        <duration>
Ran all test suites matching /review024-probe/i.

exit: 0

==========================================================================
## VERDICT
==========================================================================
candidate RED (exit 1), head GREEN (exit 0):
the probe fails where REVIEW-024 findings 1 and 2 live and passes at
the fix candidate. NON-VACUOUS by the candidate run itself (learning 14).
runner exit: 0
